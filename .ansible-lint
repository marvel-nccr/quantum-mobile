---
skip_list:
- var-naming[no-role-prefix] # Variables passed to roles (not defined in roles)
- no-changed-when # Needs more careful checking for each task -> To be removed
exclude_paths:
- submodules/
- .ansible/
