- name: playbook for the MARVEL VM
  hosts: default
  pre_tasks:
    - name: import global variables
      include_vars:
        file: globalconfig.yml
        name: globalconfig
  roles: 
    - common
    - customizations
    - editors
    - scheduler
    - simulationtools
